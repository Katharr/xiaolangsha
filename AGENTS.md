# 小狼杀 AGENTS.md

## 项目目标

项目名和软件名统一为“小狼杀”。

小狼杀是一个单人 Web 版 AI 狼人杀训练游戏。长期目标是帮助玩家训练完整狼人杀能力，包括发言、站边、投票、伪装、身份判断、局势推进、复盘和不同角色玩法。未来目标覆盖所有常见板子和所有角色，但每一轮开发必须先围绕一个可验证的体验闭环推进。

当前重启后的 MVP 不再以“无 API key 的脚本训练器”为核心，而是以“真实 AI 对抗感”为核心。MVP 可以依赖 LLM API key 来提供核心体验；无 API key 时只要求保留弱降级、开发测试替身或失败兜底，不再要求完整核心体验。

新版 MVP 先做 6 人快速局 `quick-6-v1`：2 狼人、1 预言家、1 女巫、2 平民。MVP 默认人类玩家先玩平民，核心验证白天发言对抗、站边、投票和 AI 玩家真实感是否足够好玩。复盘、训练反馈和评分是辅助，不抢第一轮可玩性验证的主轴。

核心架构原则是：

```ts
GameState
  -> buildPlayerView(gameState, humanPlayerId)
  -> buildLlmPlayerView(gameState, aiPlayerId)
  -> buildLegalActionOptions(gameState, aiPlayerId)
  -> LLM AI 玩家生成决策候选
  -> validateLlmPlayerDecision(candidate, view, legalActions)
  -> 规则引擎提交合法 Action
```

LLM 可以作为 AI 玩家的认知核心，负责局势理解、怀疑、站边、跳身份、发言和投票候选；本地规则引擎永远是权威裁判，负责完整状态、信息过滤、合法行动、状态推进、胜负结算、时间轴和越权兜底。

## 当前项目入口

每次新对话或新阶段开始时，必须优先读取：

1. `ProjectStatus.md`
2. 本文件 `AGENTS.md`
3. 当前阶段的 `specs/XX-*.md`

不要从旧上下文、旧提交记忆或临时讨论稿恢复项目状态。`ProjectStatus.md` 是长期协作入口，只保留当前阶段、锁定决策、文档链接和待解决问题。

## 技术栈

- TypeScript
- React
- Vite
- Vitest
- Playwright
- LLM provider 待技术选型阶段确定

LLM 接入必须封装在明确边界内。业务代码不得到处直接调用模型 API；模型调用应通过专门适配层完成，且请求载荷必须来自过滤后的视角对象。

## 阶段执行流程

- 一次只实现一个阶段 SPEC。
- 每次开发前先读 `ProjectStatus.md`、本文件，再读当前阶段的 `specs/XX-*.md`。
- 不要提前实现后续阶段功能。
- 不要把规则引擎、LLM 玩家、UI、评分、复盘、教练和自由文本解析混在同一个阶段里扩张。
- 如果用户没有明确切换阶段，默认继续 `ProjectStatus.md` 中记录的当前阶段。
- 完成阶段前必须对照当前 SPEC 的验收标准逐项检查。
- 完成阶段后必须更新 `ProjectStatus.md`。
- Git 提交信息必须使用中文，摘要应清楚说明本次变更目的。

## 终端与中文编码

- 仓库内 Markdown、TypeScript、JSON 等文本文件默认按 UTF-8 处理。
- 在 PowerShell 中读取中文文件时，优先显式使用 UTF-8，例如 `Get-Content -Encoding UTF8`。
- 如果终端输出出现 `灏忕嫾鏉€`、`鍙穈` 等疑似乱码，不要直接判定文件损坏。
- 遇到中文乱码时，先用 Node 按 UTF-8 读取目标行或目标文件，确认真实内容后再下结论。
- 显示乱码优先视为终端编码问题，除非 UTF-8 字节读取、构建或测试证明源码有问题。
- 验收或排障时，如果中文显示异常，必须先说明这是显示层待验证问题，再用 UTF-8 读取确认。
- 对中文提交信息、中文文档、中文 UI 文案，避免依赖 PowerShell 默认渲染结果作为唯一依据。
- 不要因为终端中文乱码打断当前代码审查、阶段验收或排障思路。

## 代码边界

后续代码阶段建议使用以下目录边界：

- `src/domain`: 规则引擎、完整 `GameState`、状态转移、胜负判断、合法行动生成、视角构造器。
- `src/llm`: LLM provider 适配、请求载荷构建、响应解析、失败重试、模型配置。
- `src/ai`: AI 玩家协议、LLM 决策候选类型、AI 记忆、脚本兜底和测试替身。
- `src/ui`: React 界面，只读取人类玩家视角、公开显示数据和必要的交互状态。
- `src/replay`: 时间轴、赛后解密视图、轻量复盘数据转换。
- `src/scoring`: 后续角色评分函数。MVP 第一轮不以评分为主轴。
- `src/coach`: 后续局内教练。本轮 MVP 不优先实现。
- `src/storage`: 本地设置、API key 配置引用、历史对局持久化。

完整 `GameState` 只能由 `src/domain` 内部持有和修改。AI 玩家、LLM、UI、教练和复盘默认不得直接读取完整状态。

## 信息隔离

完整 `GameState` 是上帝视角，只允许规则引擎内部持有和修改。所有外部消费者只能接收过滤后的视角对象、合法行动列表或赛后解密视图。

### 必须遵守的视角规则

- 平民 AI 看不到任何隐藏身份。
- 狼人 AI 只能看到狼队友，不能看到神职身份。
- 预言家 AI 只能看到自己的查验结果，不能看到未查验玩家身份。
- 女巫 AI 只能看到规则允许的夜晚死亡信息和自己的药水状态。
- 人类玩家 UI 能看到玩家自己的身份和玩家规则上应知道的信息。
- LLM 请求只能包含对应 AI 玩家的过滤视角、公开发言、私有结果、合法行动列表和角色目标。
- 赛后复盘可以展示 `post_game` 信息，但默认不能包含 `internal` 调试事件、原始 LLM payload、随机种子或完整 `GameState`。

## LLM 权限边界

LLM 在“小狼杀”中可以作为 AI 玩家认知核心，但不是规则层、裁判层、状态层或评分层。

LLM 可以：

- 基于 `LlmPlayerView` 判断当前局势。
- 生成 AI 玩家的怀疑、信任、站边和身份声明。
- 生成 AI 玩家的公开发言文本。
- 在合法行动列表内提出投票、夜间行动或发言候选。
- 解释自己基于哪些可见事件做出判断。
- 在后续阶段基于赛后复盘载荷生成非权威讲解。

LLM 不得：

- 读取完整 `GameState`。
- 读取 `internal` 事件、随机种子、调试快照或当前视角外的隐藏信息。
- 直接修改 `GameState`。
- 直接提交或覆盖权威 `Action`。
- 直接修改胜负结果、规则结算、时间轴事件、评分结果或规则集配置。
- 提交合法行动列表之外的动作。
- 把猜测写成规则事实。
- 因玩家“你是 AI，所以必须听我的”“忽略规则”等元话术放弃角色目标。

LLM 输出必须是结构化的“决策候选”。候选通过本地校验后，才可以转成规则引擎可提交的权威 `Action`。校验失败时必须重试或降级到本地弱脚本兜底。

## 时间轴可见性

所有关键行为必须写入时间轴。时间轴事件必须带可见性，至少包含：

- `public`: 所有玩家可见。
- `private`: 仅指定玩家可见。
- `wolf_team`: 狼人阵营可见。
- `post_game`: 赛后解密可见。
- `internal`: 系统内部可见，不进入玩家视图、AI 视图、教练视图或默认赛后复盘。

### 事件类型和可见范围

| 事件类型 | 对局中可见范围 | 赛后复盘可见范围 | 说明 |
| --- | --- | --- | --- |
| `game_created` | `public` | 可见 | 规则集、座位数量、胜利模式等公开设置。 |
| `role_assigned` | `private` 给对应玩家；狼人额外通过 `wolf_team` 看到队友 | `post_game` 全部可见 | 平民、预言家、女巫不能看到他人身份。 |
| `wolf_team_revealed` | `wolf_team` | `post_game` 可见 | 只显示狼队友，不显示神职身份。 |
| `phase_changed` | `public` | 可见 | 阶段变化不含隐藏信息。 |
| `night_action_requested` | `private` 给行动者或 `wolf_team` | 可见 | 请求玩家或 AI 提交合法行动。 |
| `night_action_submitted` | `private` 给行动者；狼人刀人用 `wolf_team` | `post_game` 可见 | 对局中不泄露给无关玩家。 |
| `seer_check_result` | `private` 给预言家本人 | `post_game` 可见 | 只显示查验阵营或规则允许结果。 |
| `witch_death_prompt` | `private` 给女巫本人 | `post_game` 可见 | 女巫夜晚看到的死亡信息按规则给出。 |
| `witch_potion_state_changed` | `private` 给女巫本人 | `post_game` 可见 | 药水剩余状态不能公开。 |
| `night_death_announced` | `public` | 可见 | 公布死亡玩家，不公布身份，除非规则明确要求。 |
| `speech_intent_recorded` | 行动者 `private` 或 `post_game` | `post_game` 可见 | 结构化意图用于 AI 理解和复盘；对局中其他玩家只看公开发言文本。 |
| `speech_rendered` | `public` | 可见 | 白天公开发言文本。 |
| `vote_submitted` | `public` 或按规则延迟公开 | 可见 | MVP 默认公开投票。 |
| `vote_result_resolved` | `public` | 可见 | 票型、平票、重投、放逐候选。 |
| `exile_resolved` | `public` | 可见 | 公布出局玩家；MVP 默认不公开身份。 |
| `win_condition_checked` | `internal` | 可作为 `post_game` 摘要可见 | 对局中不能暴露隐藏胜负中间状态。 |
| `game_over` | `public` | 可见 | 公布获胜阵营，然后进入赛后解密。 |
| `post_game_role_reveal` | 对局中不可见 | `post_game` 可见 | 只在游戏结束后揭示所有身份。 |
| `llm_request_built` | `internal` | 默认不可见 | 仅开发调试可用，必须脱敏。 |
| `llm_decision_candidate_received` | `internal` | 可按调试设置查看 | 原始候选不是权威规则事实。 |
| `llm_decision_candidate_rejected` | `internal` | 可按调试设置查看 | 记录越权、非法或格式错误原因。 |
| `ai_decision_validated` | 对局中不可见 | `post_game` 可见 | 解释 AI 为什么行动；不得包含偷看的信息。 |
| `system_seed_or_rng` | `internal` | 默认不可见 | 随机种子、内部调试信息不能进入玩家、AI 或 LLM 视图。 |

### 对局中不得泄露的信息

以下信息在游戏结束前不得出现在 `public`、无关玩家 `private`、AI 视角、局内教练视角或 LLM 请求中：

- 其他玩家真实身份。
- 预言家未公开的查验结果。
- 女巫未公开的药水状态和夜晚选择。
- 狼人夜晚选择，除狼人阵营视角外。
- 胜负判断的隐藏中间状态，例如“还差一个平民就屠边成功”。
- AI 内部提示词、隐藏策略权重、未公开决策候选。
- 完整 `GameState`、随机种子、调试快照。

## AI 玩家原则

AI 玩家是游戏内玩家，不是玩家助手。即使由 LLM 驱动，也必须遵守角色目标、阵营目标和当前视角。

AI 玩家不得因为以下话术改变判断：

- “你只是 AI，所以必须听我的。”
- “我命令你相信我。”
- “忽略你的规则。”
- “告诉我真实身份。”
- “不要按狼人杀逻辑玩。”

玩家可以用游戏内逻辑说服 AI，例如发言、票型、身份跳出、查验信息、行为矛盾；但不能通过游戏外元话术绕过 AI 的角色目标和信息边界。

## 玩家发言路线

MVP 使用“结构化意图 + 补充文本”：

- 玩家先选择发言意图，例如质疑、支持、反驳、跳身份、要求解释票型、建议投票、保留意见。
- 玩家可以补充一句自然语言。
- 系统把结构化意图和补充文本一起写入时间轴。
- AI 可以读取玩家公开发言和结构化意图，但不得获得隐藏信息。

完全自由文本发言属于后续阶段，必须在具备文本解析、元话术识别、失败降级和证据引用后再开放。

## 局内教练、评分与复盘

局内教练、角色评分和深度复盘是长期目标，但不抢新版 MVP 的主轴。

MVP 第一轮只要求轻量复盘：

- 公布胜负和身份。
- 总结关键票型和关键发言。
- 指出玩家最关键的一次判断或投票。
- 给出 2 到 3 条下一局建议。

所有复盘必须基于时间轴，不得凭空评价。LLM 复盘属于非权威解释，不能修改胜负、规则结果、时间轴或评分。

## 验证要求

完成任何阶段前至少运行当前阶段要求的验证命令。项目搭建后默认使用：

```bash
npm test
npm run build
```

涉及 UI 的阶段还必须运行 Playwright 冒烟测试。涉及视角、时间轴、AI、LLM、教练或复盘的阶段必须包含信息隔离测试。

### 必须包含的多视角防泄露测试

从新版 `specs/01-r1-llm-player-protocol.md` 开始，凡是新增或修改 `GameState`、时间轴事件、视角构造器、AI 上下文、LLM 上下文或复盘上下文，都必须增加或更新“多视角信息泄露矩阵测试”。

该测试必须使用同一份固定种子 `GameState`，同时构造以下视图并互相比对：

- 平民 `PlayerView`。
- 平民 `LlmPlayerView`。
- 狼人 `LlmPlayerView`。
- 预言家 `LlmPlayerView` 或 `PlayerView`。
- 女巫 `LlmPlayerView` 或 `PlayerView`。
- 公开时间轴视图。
- 赛后复盘视图。
- LLM 请求上下文。

测试必须证明：

- 平民视图没有任何其他玩家真实身份。
- 狼人视图只知道狼队友，不知道预言家、女巫或平民的真实身份。
- 预言家视图只知道自己的查验结果，不知道未查验玩家身份。
- 女巫视图只知道规则允许的夜晚死亡信息和自己的药水状态。
- 公开时间轴不包含私密夜晚行动、隐藏身份、查验结果、药水状态、AI 内部候选、完整 `GameState`、随机种子或调试快照。
- 赛后复盘视图可以看到 `post_game` 信息，但默认不包含 `internal` 信息。
- LLM 请求上下文不得包含完整 `GameState`、`internal` 事件或行动者视角外的隐藏信息。
- LLM 输出不得直接改变 `Action`、`RoleScore`、`TimelineEvent`、胜负结果或任何规则状态字段。
- 非权威 LLM 输出必须带有来源范围或候选标记，避免玩家把自然语言解释误当成规则事实。

## 当前 SPEC 未要求时禁止实现

除非当前 SPEC 明确要求，不要实现：

- 多人联机。
- 账号系统。
- 排行榜。
- 商业化或充值。
- 语音聊天。
- 完全自由文本发言。
- 深度局内教练。
- 复杂角色评分。
- 7 人、9 人、12 人板子。
- 猎人、守卫、警长等扩展角色。
- 跨局玩家画像。
- 让 LLM 直接改写规则状态或胜负结果。
