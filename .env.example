# 小狼杀 AI 代理配置模板
#
# 用法：复制本文件为 .env 并填入你自己的凭证。
#   cp .env.example .env
#
# 安全：.env 已加入 .gitignore，绝不提交。这些变量只在本地 Vite dev-server
# 进程内被读取（src/ai-proxy），API key 永不下发到浏览器、永不进 git。
# 不要使用 / 不要碰 Codex 的 C:/vibecoding/auth.json。

# OpenAI 兼容端点的 base url（需包含到 /v1，结尾不带斜杠）。
# 参考：https://fuck-clau.de/v1
AI_BASE_URL=

# 你的 API Key。
AI_API_KEY=

# —— 按任务分三档模型（质量 vs 速度），url/key 全部共用同一端点 ——
# 本项目使用的 fuck-clau.de 端点实测可调用：gpt-5.4-mini、gpt-5.4、gpt-5.5
#   （gpt-5.2 系列在 Codex 通道返回 400，不可用；用 GET /v1/models 看你自己 key 的清单，
#    但「列表里有」≠「能调用」，建议实际发一次最小请求确认）。

# 发言类（发言/拉票/遗言）：最频繁、要快+自然——gpt-5.4 又快(~1.6s)又比 mini 强、不超时。
AI_SPEECH_MODEL=gpt-5.4

# 推理类（投票/夜晚行动/女巫/猎人）：决策逻辑质量优先——gpt-5.5 最强（较慢，配合下方 90s 超时）。
AI_REASONING_MODEL=gpt-5.5

# 复盘问答（review_question）：频率低、可慢、要质量——gpt-5.5。
AI_REVIEW_MODEL=gpt-5.5

# 兜底：未设上面 SPEECH/REASONING 时回退到它（兼容旧配置）。
AI_MODEL=gpt-5.4-mini

# 线格式：chat = Chat Completions（默认，最通用）；responses = OpenAI Responses API。
# 若你的端点只支持 responses，改成 responses。
AI_WIRE_API=chat

# 单次 LLM 调用超时（毫秒），默认 90000。推理用 gpt-5.5 偏慢，过低会频繁超时降级到脚本。
AI_TIMEOUT_MS=90000
