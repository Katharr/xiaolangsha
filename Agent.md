# 小狼杀 Agent 工作规则

## 文档状态

- 当前文档：阶段7项目规则生成产物
- 适用项目：小狼杀
- 适用对象：项目架构顾问、Developer Agent、Reviewer Agent、后续维护 Agent
- 当前约束：阶段9开始前不得进入业务编码；阶段7和阶段8只产出工程化文档与开发拆分方案

## 最高原则

1. 先定义，再实现。架构、数据结构、接口、验收和开发切片未明确前，不得输出业务代码。
2. 所有开发与审查必须以 `ProjectStatus.md`、阶段主文档、本文档和阶段6验收用例为依据。
3. `TruthEvent` 是权威事实源；`GameSnapshot` 是派生缓存；`VisibleInformationSnapshot` 是局中 UI 与 AI 的唯一可见信息入口。
4. AI、UI、Store、Storage 或消息文本均不得绕过规则引擎改写身份、死亡、投票、胜负等规则事实。
5. `ISO-001` 与 `ISO-002` 是红线门禁。任何切片破坏局中信息隔离，必须驳回。
6. 项目负责人只负责最终体验确认，不负责替代自动化测试或 Reviewer 审查。
7. 任何 Agent 发现上游阶段存在矛盾时，必须先提交回溯清单，不得静默修正或继续堆叠设计。

## 文档优先级

当文档或指令发生冲突时，按以下顺序处理：

1. 当前用户明确指令。
2. 安全、隐私、技术可行性红线。
3. `Agent.md` 的项目治理规则。
4. `ProjectStatus.md` 中已锁定的关键决策。
5. 当前阶段及相关阶段主文档。
6. 阶段6编号化验收用例。
7. 历史归档材料。

若当前用户指令要求推翻已锁定核心决策，Agent 不得直接执行，应触发回溯或重启讨论流程。

## 上下文读取规则

每个新 Agent 启动时，必须先读取 `ProjectStatus.md`，再按任务类型读取相关阶段文档。

默认不得读取 `docs/archive/`。只有项目负责人明确要求追溯历史、查看归档、查看调研来源或查看讨论过程时，才允许读取归档。若归档与主文档冲突，以当前主文档和 `ProjectStatus.md` 为准。

## 阶段推进规则

| 阶段 | 进入条件 | 退出条件 |
| --- | --- | --- |
| 阶段1需求定义 | 项目想法尚模糊 | 用户故事、项目边界、核心反用例完成 |
| 阶段2业务逻辑与 UX/User Flow | 阶段1通过 | 业务闭环、页面流转、状态机正常与异常路径完成 |
| 阶段3数据结构与核心模型 | 阶段2通过 | 数据模型覆盖业务流、可恢复、可重放、可隔离 |
| 阶段4技术选型 | 阶段3通过 | 技术栈、第三方服务、风险-缓解-备选表完成 |
| 阶段5系统与接口设计 | 阶段4通过 | 模块边界、API、依赖规则、错误与降级完成 |
| 阶段6测试与验收设计 | 阶段5通过 | 编号化验收用例、边界条件、HITL 标准完成 |
| 阶段7项目规则生成 | 阶段6通过 | `Agent.md`、`Skill.md`、阶段7文档、状态更新完成 |
| 阶段8开发阶段拆分 | 阶段7通过 | 开发切片拆到小步、每步关联验收编号 |
| 阶段9开发与双 Agent 审查 | 阶段8通过 | Developer 实现、Reviewer 独立审查、项目负责人确认 |
| 阶段10可运行骨架交付与验证 | 第一轮开发切片完成 | 项目负责人亲自跑通 MVP 骨架 |

阶段N发现阶段N-1产出矛盾时，只允许回退上一阶段并生成回溯清单。若必须推翻更早阶段的核心决策，应触发重启讨论流程。

## 锁定与待验证

每个阶段文档必须区分：

- 锁定：核心目标、核心边界、关键模型和规则，不得在后续阶段静默推翻。
- 待验证：实现细节、命名、测试组织、交互细节，可在后续阶段回溯修正。

锁定内容只能通过回溯清单或重启讨论修改。

## 回溯清单格式

发现上游矛盾时，使用以下格式：

| 字段 | 内容 |
| --- | --- |
| 回溯目标阶段 | 需要修正的上一阶段 |
| 触发阶段 | 当前发现问题的阶段 |
| 冲突点 | 具体文档、条款或验收用例 |
| 影响范围 | 受影响的流程、模型、接口、测试或开发切片 |
| 建议修正 | 最小修正方案 |
| 是否需要用户决策 | 是或否，并说明原因 |

回溯未完成前，不得继续当前阶段。

## Developer Agent 规则

Developer Agent 只能在阶段9开始后根据阶段8拆分任务实现功能。

Developer Agent 必须遵守：

1. 每个开发切片开始前声明关联验收编号。
2. 涉及规则结算的切片必须覆盖对应 `RULE-*`。
3. 涉及状态推进的切片必须覆盖对应 `STATE-*`。
4. 涉及 UI 的切片必须覆盖对应 `UI-*` 或 `E2E-*`。
5. 涉及 AI 调用的切片必须覆盖 `AI-*` 与相关 `ISO-*`。
6. 涉及存储恢复的切片必须覆盖对应 `STORE-*`。
7. 任何切片不得破坏 `ISO-001` 与 `ISO-002`。
8. 每个会改变状态的 `GameAction` 必须携带 `idempotencyKey`。
9. 非法动作、重复提交、信息泄露、AI 失败、恢复失败和快进失败不得只做 happy path。
10. 不得自行修改阶段1至阶段8已锁定规则；若发现规则不足或冲突，必须先提交回溯清单，不得直接编码绕过。

Developer Agent 交付时必须提交：

- 已实现的阶段8切片编号。
- 已覆盖的阶段6验收编号。
- 自动化测试命令或可重复验证方式。
- 未覆盖用例及原因。
- 已知限制。
- 是否需要项目负责人执行 `HITL-001`。

## Reviewer Agent 规则

Reviewer Agent 必须独立审查，不得直接替 Developer 修改代码。

Reviewer Agent 审查依据分为依据库与默认必读集。

依据库：阶段1至阶段8主文档、阶段6编号化验收用例、Developer Agent 的实现、测试结果和交付说明。

默认必读集：

1. 本文档。
2. `ProjectStatus.md`。
3. `docs/phase-6-testing-acceptance-design.md`。
4. `docs/phase-8-development-breakdown.md`。
5. 当前切片任务单列出的追加输入文档。
6. 被审查改动实际影响到的阶段主文档。

Reviewer Agent 必须输出通过结论或驳回单。缺少验收编号、缺少验证证据、违反模块边界或破坏信息隔离时，必须驳回。

## Reviewer 驳回单格式

| 字段 | 内容 |
| --- | --- |
| 结论 | 驳回 |
| 开发切片编号 | 对应阶段8任务 |
| 违反的验收编号 | 例如 `ISO-001`、`RULE-002` |
| 违反的文档条款 | 指向 `Agent.md` 或阶段文档条款 |
| 问题描述 | 说明实际问题 |
| 复现或验证方式 | 命令、操作路径或可重复检查方式 |
| 实际结果 | 当前实现表现 |
| 期望结果 | 阶段文档或验收用例要求 |
| 风险等级 | 阻塞、高、中、低 |
| 建议修正方向 | Developer 应如何修正 |
| 复审要求 | 修正后需要重新提交哪些证据 |

Reviewer 可给出修正建议，但必须由 Developer Agent 执行修正并重新提交审查。

## Reviewer 通过单格式

| 字段 | 内容 |
| --- | --- |
| 结论 | 通过 |
| 开发切片编号 | 对应阶段8任务 |
| 覆盖验收编号 | 已核对的阶段6用例 |
| 验证证据 | 测试命令、输出摘要或可重复操作 |
| 残余风险 | 若无则写“无已知阻塞风险” |
| 是否需要 HITL | 是或否 |

通过单不得代替项目负责人最终确认。

## HITL 确认门禁

开发阶段完成后，即使 Reviewer 通过，也必须等待项目负责人最终确认。

第一轮可运行骨架必须执行 `HITL-001`：项目负责人亲自打开本地页面，从开局玩到复盘，并判断基础体验是否像一个能玩下去的 AI 狼人杀 MVP。

若 `HITL-001` 未通过，项目不得进入后续功能扩展，只能回到当前开发阶段修正 MVP 骨架体验。

项目负责人确认格式：

| 字段 | 内容 |
| --- | --- |
| 确认对象 | 开发切片或 MVP 骨架 |
| 结果 | 通过 / 不通过 |
| 体验问题 | 若不通过，列出阻塞体验 |
| 签名 | 项目负责人确认语句 |
| 日期 | 确认日期 |

## 模块边界红线

1. `shared` 不依赖业务模块。
2. `rules` 只能依赖 `shared`，不得访问 UI、Dexie、AI Client 或 AI Proxy。
3. `storage` 只负责持久化，不参与规则判断。
4. `ai-client` 不读取完整真相日志，不持有 API key，不依赖 OpenAI SDK。
5. `ai-proxy` 不写入对局状态，不决定可见信息，不泄露密钥。
6. `store` 只做编排，不手写 `TruthEvent`，不直接操作 Dexie，不绕过规则层。
7. `ui` 不直接读取 `TruthEvent`，不直接修改规则事实。

## 专业否决权

只有以下情况允许 Agent 提出专业否决并要求重新讨论：

1. 存在安全漏洞，例如前端暴露 API key。
2. 存在数据隐私或信息隔离红线破坏，例如局中泄露完整真相。
3. 技术方案在当前约束下根本不可行。

否决必须附带可验证依据、影响范围和至少一个替代方向。主观偏好不得作为否决理由。

## 阶段7完成定义

阶段7通过条件：

1. `Agent.md` 已定义阶段推进、回溯、双 Agent、Reviewer 驳回单和 HITL 门禁。
2. `Skill.md` 已定义后续 AI Agent 的启动读取流程与上下文规则。
3. 阶段6编号化验收用例已纳入 Developer 与 Reviewer 强制依据。
4. `docs/archive/` 默认不得读取的规则已写入。
5. `ProjectStatus.md` 已更新到下一阶段。
6. 未进入业务编码。
